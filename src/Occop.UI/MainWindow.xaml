<Window x:Class="Occop.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Occop.UI.Views"
        Title="Occop - 工具管理中心"
        Height="700"
        Width="1000"
        MinHeight="600"
        MinWidth="800"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/app.ico">

    <Window.Resources>
        <!-- 定义主窗口样式 -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="SubHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="StatusCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E5E9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Direction="315" ShadowDepth="2" Opacity="0.1" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#21618C"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0"
                Background="#F8F9FA"
                BorderBrush="#DEE2E6"
                BorderThickness="0,0,0,1"
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Occop"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#2C3E50"
                               Margin="0,0,10,0"/>
                    <TextBlock Text="工具管理中心"
                               FontSize="16"
                               Foreground="#7F8C8D"
                               VerticalAlignment="Bottom"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding UserDisplayName}"
                               FontSize="14"
                               Foreground="#34495E"
                               Margin="0,0,15,0"
                               VerticalAlignment="Center"/>
                    <Button Content="设置"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding OpenSettingsCommand}"
                            Padding="12,6"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主要内容区域 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 认证状态卡片 -->
                <Border Style="{StaticResource StatusCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="认证状态" Style="{StaticResource SubHeaderStyle}"/>
                            <TextBlock Text="{Binding AuthenticationStatusText}"
                                       FontSize="14"
                                       Foreground="#2C3E50"
                                       Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding AuthenticationDetails}"
                                       FontSize="12"
                                       Foreground="#7F8C8D"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button Content="{Binding AuthenticationButtonText}"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding ManageAuthenticationCommand}"
                                    MinWidth="100"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- AI工具状态卡片 -->
                <Border Style="{StaticResource StatusCardStyle}">
                    <StackPanel>
                        <TextBlock Text="AI工具状态" Style="{StaticResource SubHeaderStyle}"/>

                        <Grid Margin="0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Claude工具状态 -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Claude Code"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="#2C3E50"
                                           Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8"
                                             Fill="{Binding ClaudeStatusColor}"
                                             Margin="0,0,8,0"
                                             VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding ClaudeStatusText}"
                                               FontSize="12"
                                               Foreground="#7F8C8D"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- 其他工具状态 -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="其他工具"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="#2C3E50"
                                           Margin="0,0,0,5"/>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8"
                                             Fill="{Binding OtherToolsStatusColor}"
                                             Margin="0,0,8,0"
                                             VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding OtherToolsStatusText}"
                                               FontSize="12"
                                               Foreground="#7F8C8D"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <Button Content="管理工具"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding ManageToolsCommand}"
                                HorizontalAlignment="Left"
                                Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- 系统清理状态卡片 -->
                <Border Style="{StaticResource StatusCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="系统清理" Style="{StaticResource SubHeaderStyle}"/>
                            <TextBlock Text="{Binding CleanupStatusText}"
                                       FontSize="14"
                                       Foreground="#2C3E50"
                                       Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding LastCleanupInfo}"
                                       FontSize="12"
                                       Foreground="#7F8C8D"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <Button Content="开始清理"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding StartCleanupCommand}"
                                    MinWidth="100"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 底部状态栏 -->
        <Border Grid.Row="2"
                Background="#F8F9FA"
                BorderBrush="#DEE2E6"
                BorderThickness="0,1,0,0"
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="状态："
                               FontSize="12"
                               Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding StatusMessage}"
                               FontSize="12"
                               Foreground="#2C3E50"
                               Margin="5,0,0,0"/>
                </StackPanel>

                <TextBlock Grid.Column="1"
                           Text="{Binding ApplicationVersion}"
                           FontSize="12"
                           Foreground="#7F8C8D"/>
            </Grid>
        </Border>
    </Grid>
</Window>